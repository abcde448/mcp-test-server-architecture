## Tools Layer

This directory will contain individual MCP tool implementations.

### Planned Tools
- add_numbers (test tool)
- dummy_tool (for validation)

Each tool will be implemented as a separate function
and registered with the MCP server.
